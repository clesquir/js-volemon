language: node_js
node_js:
  - "10"
before_install:
  - curl https://install.meteor.com | /bin/sh
  - export PATH="$HOME/.meteor:$PATH"
before_script:
  - meteor npm install
cache:
  directories:
    - $HOME/.meteor
    - $HOME/.npm
    - node_modules
